--- a/lara/funcs/keepalive.swift
+++ b/lara/funcs/keepalive.swift
@@ -5,24 +5,32 @@
 //  Created by ruter on 29.03.26.
 //
+//  Hardened: handles audio-session interruptions (calls/Siri/alarms) and route
+//  changes, auto-restarts playback if it ever stops, and keeps the session
+//  active so iOS does not suspend the app mid-session (the "dies after ~10 min"
+//  symptom when the app is backgrounded).
+//
 
 import AVFoundation
 
 private var kaplayer: AVAudioPlayer?
+private var kaobservers: [NSObjectProtocol] = []
 var kaenabled = false
 
 func toggleka() {
     if kaenabled {
-        kaplayer?.stop()
-        kaplayer = nil
-        kaenabled = false
-        globallogger.log("(ka) disabled keepalive")
-        
+        stopka()
         return
     }
-    
+    startka()
+}
+
+private func startka() {
     do {
         let session = AVAudioSession.sharedInstance()
+        // .playback keeps the process alive in the background (UIBackgroundModes
+        // already declares "audio"). .mixWithOthers avoids hijacking the user's
+        // music while still holding the session open.
         try session.setCategory(.playback, mode: .default, options: [.mixWithOthers])
-        try session.setActive(true)
+        try session.setActive(true, options: [])
     } catch {
         globallogger.log("(ka) audio session failed: \(error)")
@@ -31,20 +39,93 @@
 
     let fileurl = getwavurl()
-    
     if !FileManager.default.fileExists(atPath: fileurl.path) {
         makesilentwav(at: fileurl)
     }
-    
+
     do {
         kaplayer = try AVAudioPlayer(contentsOf: fileurl)
-        kaplayer?.numberOfLoops = -1
-        kaplayer?.volume = 0.0
+        kaplayer?.numberOfLoops = -1      // loop forever
+        // Tiny non-zero volume: the WAV is digital silence anyway, but a non-zero
+        // volume guarantees iOS treats the player as actively producing audio.
+        kaplayer?.volume = 0.01
         kaplayer?.prepareToPlay()
         kaplayer?.play()
         kaenabled = true
+        registerkaobservers()
         globallogger.log("(ka) enabled keepalive")
     } catch {
         globallogger.log("(ka) audio failed: \(error)")
     }
+}
+
+private func stopka() {
+    removekaobservers()
+    kaplayer?.stop()
+    kaplayer = nil
+    kaenabled = false
+    try? AVAudioSession.sharedInstance().setActive(false, options: [.notifyOthersOnDeactivation])
+    globallogger.log("(ka) disabled keepalive")
+}
+
+// Resume playback if it ever stalls (defensive — should never be needed, but a
+// single stalled frame used to cost the whole background session).
+private func ensurekaplaying() {
+    guard kaenabled, let p = kaplayer else { return }
+    if !p.isPlaying {
+        p.prepareToPlay()
+        p.play()
+    }
+}
+
+private func registerkaobservers() {
+    removekaobservers()
+    let nc = NotificationCenter.default
+
+    // Interruptions: phone call, Siri, alarm, etc. Resume when they end.
+    let intr = nc.addObserver(forName: AVAudioSession.interruptionNotification,
+                              object: nil, queue: .main) { note in
+        guard let info = note.userInfo,
+              let raw = info[AVAudioSessionInterruptionTypeKey] as? UInt,
+              let type = AVAudioSession.InterruptionType(rawValue: raw) else { return }
+        if type == .ended {
+            let optsRaw = info[AVAudioSessionInterruptionOptionKey] as? UInt ?? 0
+            let opts = AVAudioSession.InterruptionOptions(rawValue: optsRaw)
+            try? AVAudioSession.sharedInstance().setActive(true, options: [])
+            if opts.contains(.shouldResume) || true {
+                ensurekaplaying()
+            }
+            globallogger.log("(ka) resumed after interruption")
+        }
+    }
+
+    // Route changes: headphones pulled, bluetooth drop, etc.
+    let route = nc.addObserver(forName: AVAudioSession.routeChangeNotification,
+                               object: nil, queue: .main) { _ in
+        try? AVAudioSession.sharedInstance().setActive(true, options: [])
+        ensurekaplaying()
+    }
+
+    // Media services reset (rare): the whole session was torn down — rebuild.
+    let reset = nc.addObserver(forName: AVAudioSession.mediaServicesWereResetNotification,
+                               object: nil, queue: .main) { _ in
+        globallogger.log("(ka) media services reset — rebuilding keepalive")
+        stopka()
+        startka()
+    }
+
+    // App returning to foreground: make sure we're still ticking.
+    let fg = nc.addObserver(forName: UIApplication.didBecomeActiveNotification,
+                            object: nil, queue: .main) { _ in
+        try? AVAudioSession.sharedInstance().setActive(true, options: [])
+        ensurekaplaying()
+    }
+
+    kaobservers = [intr, route, reset, fg]
+}
+
+private func removekaobservers() {
+    let nc = NotificationCenter.default
+    for o in kaobservers { nc.removeObserver(o) }
+    kaobservers.removeAll()
 }
 
@@ -55,24 +136,26 @@
 
 private func makesilentwav(at url: URL) {
+    // Longer buffer (5s) so the loop boundary is hit less often — fewer chances
+    // for a scheduling gap to stall the player.
     let samplerate = 44100
-    let duration = 1
+    let duration = 5
     let numsamples = samplerate * duration
-    
+
     var wavdata = Data()
-    
+
     let byterate = samplerate * 2
     let blockalign: UInt16 = 2
     let datasize = numsamples * 2
     let chunksize = 36 + datasize
-    
+
     func append<T>(_ value: T) {
         var v = value
         wavdata.append(Data(bytes: &v, count: MemoryLayout<T>.size))
     }
-    
+
     wavdata.append("RIFF".data(using: .ascii)!)
     append(UInt32(chunksize))
     wavdata.append("WAVE".data(using: .ascii)!)
-    
+
     wavdata.append("fmt ".data(using: .ascii)!)
     append(UInt32(16))
@@ -83,10 +166,10 @@
     append(blockalign)
     append(UInt16(16))
-    
+
     wavdata.append("data".data(using: .ascii)!)
     append(UInt32(datasize))
-    
+
     wavdata.append(Data(count: datasize))
-    
+
     try? wavdata.write(to: url)
 }
